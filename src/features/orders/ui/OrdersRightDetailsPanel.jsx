import { memo } from "react";

const OrdersRightDetailsPanel = memo(function OrdersRightDetailsPanel({
  selectedOrderId,
  deferredSelectedOrderId,
  selectedOrder,
}) {
  void selectedOrderId;
  void deferredSelectedOrderId;
  void selectedOrder;
  return null;
});

export default OrdersRightDetailsPanel;
